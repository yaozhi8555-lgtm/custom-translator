[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "custom-translator"
version = "0.1.0"
requires-python = ">=3.13"

[project.scripts]
translate = "translator.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}